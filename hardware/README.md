# Bibliotèque hardware

Les en-têtes se trouvenr dans `include/`, la bibliothèque dans `lib/`, et la configuration par défaut dans `etc/hardware.ini`.

Vous trouverez un exemple d'utilisation du timer dans `examples/`.

